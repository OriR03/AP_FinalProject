/**
 * this package is the server package, which has the parser class which gets a package and analyzes it
 * while also having MyHttpServer which is responsible for starting the server of the project
 */

package biu_project.server;